class LightningIphone {
    chargeWithLightning() {
        console.log("Charging iPhone via Lightning... (JavaScript)");
    }
}

// Для проверки отдельно:
const phone = new LightningIphone();
phone.chargeWithLightning();
